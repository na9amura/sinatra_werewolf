<form method="post" action="/player/see/" id="see_form">
  <div>
    <label><%= @seer.name %> さんが選択したのは</label>
  </div>
  <select name="seen">
    <% @players.each { |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
    <% } %>
  </select>
	<input type="submit" value="決定">
</form>

<div>
	<a href="/player/select/bite/">SKIP</a>
</div>
